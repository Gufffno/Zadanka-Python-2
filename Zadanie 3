print("Python posiada specjalny typ danych logicznych, który jest używany w instrukcjach warunkowych i pętlach. Wartości logiczne True albo False są najczęściej zwracane, kiedy porównujemy ze sobą dwie wartości.")

liczba = 7

if liczba > 0:
    print(liczba, " > 0")

if liczba < 0:
    print(liczba, " < 0")

if liczba == 0:
    print(liczba, "== 0")

if liczba != 0:
    print(liczba, "!= 0")

print("https://www.learnpython.org/pl/Instrukcje_warunkowe")

